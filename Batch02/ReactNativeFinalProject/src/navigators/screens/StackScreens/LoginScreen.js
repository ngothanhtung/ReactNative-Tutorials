import React from 'react';
import LoginContainer from '../../../modules/AuthModule/containers/LoginContainer';

const LoginScreen = ({ navigation }) => {
  return (
    <LoginContainer navigation={navigation} />
  );
}

export default LoginScreen;
