import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		color: 'blue',
		fontWeight: 'bold',
		fontSize: 30,
	},

	text: {
		color: 'red',
	},
});

export default styles;
